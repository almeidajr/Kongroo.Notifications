apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  namespace: kongroo
  labels:
    app.kubernetes.io/name: notifications-api
    app.kubernetes.io/instance: notifications-api
    app.kubernetes.io/version: "0.0.3"
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: kongroo
    app.kubernetes.io/managed-by: kubectl
spec:
  selector:
    app: notifications-api
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
